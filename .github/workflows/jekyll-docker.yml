name: Sync CI

on:
  workflow_dispatch:
  push:
    branches: [ "master" ]

env:
  LICENSE_KEY: ${{ secrets.GEOLITE_LICENSE_KEY }}
  GITHUB_USER: ${{ github.actor }}
  GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Checkout & Install
      run: sudo apt-get clean all && sudo apt-get -qq -y update && sudo apt-get install -y axel && chmod 755 sync.sh
     
    - name: Sync
      run: ./sync.sh
      
      
